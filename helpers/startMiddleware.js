// const { app, express } = require("./imports");

// const startMiddleware = () => {
//   // Body-Parsing Middleware
//   app.use(express.json());
//   app.use(express.urlencoded({ extended: true }));

//   // "Pointer" Middleware
//   app.use(express.static("public"));
// };

// module.exports = { startMiddleware };
